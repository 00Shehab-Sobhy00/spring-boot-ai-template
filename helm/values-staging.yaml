# Staging overrides ONLY — anything identical to defaults stays in values.yaml.
replicaCount: 2
ingress:
  enabled: true
  host: service-template.staging.example.com
resources:
  requests: { cpu: 100m, memory: 384Mi }
  limits:   { cpu: 500m, memory: 768Mi }
