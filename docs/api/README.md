# API Documentation

- `error-catalog.md` — the 4xx/5xx taxonomy and internal-vs-external classification every service
  follows.
- `conventions.md` — URL/versioning/pagination conventions (summary of `ai/patterns/rest-api.md` for
  API consumers).
- Per-service OpenAPI specs live with each service; link or export them here if the platform
  aggregates them.
