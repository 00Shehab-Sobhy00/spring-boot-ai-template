# Database Documentation

- `migrations.md` — how schema changes are made (Liquibase workflow).
- `schema-conventions.md` — naming, types, indexing conventions.
- Per-service schema notes/ERDs can live here as `<service>-schema.md` when a service's model is
  complex enough to need one.
