# Deployment Documentation

- `onboarding.md` — first day: run it locally, what to read, in what order.

- `environments.md` — the environment matrix (local/staging/prod) and what differs.
- `networking.md` — proxies, mTLS, mesh behavior; the "why does staging behave differently" page.
- `runbook.md` — deploy/rollback/incident basics.
- Helm chart reference: `/helm/README.md`. Raw manifests (if any bypass Helm): `/k8s/`.
